# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 서명 개인키. 공개키(.key.pub)는 커밋해도 되지만 개인키는 절대 안 된다.
# 실제 키는 저장소 바깥(~/.tauri/)에 두므로 여기 걸릴 일이 없어야 정상이고,
# 이 규칙은 실수로 복사해 넣었을 때를 위한 이중 안전장치다.
*.key
*.pem
*.pfx
*.p12

# 검증 에이전트가 남기는 실행 화면 캡처
discan-r*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
